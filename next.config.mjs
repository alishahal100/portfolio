/** @type {import('next').NextConfig} */
const nextConfig = {
    // next.config.mjs
 
    images: {
      domains: ['assets.aceternity.com'],
    
  },
  
};
// next.config.mjs

  
  

export default nextConfig;
